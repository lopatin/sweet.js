import Term from './term-spec';

function transform(reducer, term) {
  // if (List.isList(term)) {
  //
  // } else if ()
  // Term.spec[term.type]
}

export default function reduce(reducer, term) {
  return transform(reducer, term);
}
